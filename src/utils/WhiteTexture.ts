import { Texture } from "pixi.js";

const white = Texture.WHITE;
/*Texture.from({
    resource: new Uint8Array([255, 255, 255, 255]),
    width: 1,
    height: 1
});*/

export const WHITE_TEXTURE = white;
